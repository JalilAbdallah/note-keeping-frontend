{
  "name": "note-keeping-frontend",
  "version": "1.0.0",
  "description": "Frontend for the Note Keeping App",
  "scripts": {
    "start": "live-server ./ --port=8080 --entry-file=src/index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}